# MapPi3 V1.2.23 Route Search + Weather Origin Prep

## Added

- Route quick filters: near me, sunrise, sunset, water, easy, moderate, shelter, overlook.
- Route search suggestion chips generated from route names, places, difficulty, type, tags, waypoints, and POIs.
- Hero `use current GPS` button reuses the phone-location origin flow.
- Open-Meteo weather now uses the saved phone GPS origin when available, otherwise local/home reference.
- Travel note now clearly labels straight-line distance and speed-based rough drive as placeholders until real road routing / Supabase catalog data is wired.

## Supabase placeholder

No Supabase credentials or endpoints are embedded. Future sync should read public anon URL/key from environment/config only after CAK3D provides them.
